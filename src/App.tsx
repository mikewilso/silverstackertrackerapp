import './App.css';
import React, { FC } from 'react';
import { AppContainer } from './components/AppContainer';

export const App: FC = () => {
    return (
        <div className='App'>
            <AppContainer></AppContainer>
        </div>
    );
};

export default App;
